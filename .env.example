# Server Configuration
PORT=7861
HOST=0.0.0.0
NODE_ENV=production

# Security
# Generate a strong random string for JWT_SECRET (min 32 chars)
# You can generate one with: openssl rand -hex 32
JWT_SECRET=

# Admin Credentials (optional - auto-generated if not provided)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=

# Database
# DATABASE_PATH=./data/chatflowui.db

# Rate Limiting
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=100

# CORS
# Comma-separated list of allowed origins for Admin API (if needed in prod)
# ADMIN_ORIGIN=https://admin.yourdomain.com
